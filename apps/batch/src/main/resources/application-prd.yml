spring:
  data:
    redis:
      host: ${REDIS_MASTER_HOST}
      port: ${REDIS_MASTER_PORT}

alert:
  slack:
    webhook-url: ${SLACK_WEBHOOK_URL:}

# notification.email.*은 application.yml(default)에서 환경변수로 주입한다.

logging:
  level:
    root: INFO
    io.premiumspread: INFO
